export default function HistoryPage() {
  return (
    <section className="rounded border border-border-subtle bg-bg-card p-4 text-sm text-text-secondary">
      Trade and order history need a paginated endpoint on the engine. Nothing to show yet.
    </section>
  );
}
